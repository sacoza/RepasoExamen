{% extends 'base.html.twig' %}

{% block body %}
    <h1>Justicium creation</h1>

    {{ form_start(form) }}
        {{ form_row(form.username) }}
        {{ form_row(form.plainPassword.first) }}
        {{ form_row(form.plainPassword.second) }}
        {{ form_row(form.superpoder) }}

    <button type="submit">Register!</button>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('justicia_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
